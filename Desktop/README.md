ColorTIE Command Line Application
=================================
+ Usage : ./ColorTie filepath k deltaz epsilon
    - k, deltaz, epsilon are algorithm parameters

###Compilation
+ Build using cmake
+ do `cmake .`, then `make`

